
// const margin = { top: 10, bottom: 10, left: 120, right: 20 };
// const width = 800 - margin.left - margin.right;
// const height = 600 - margin.top - margin.bottom;
// // Creates sources <svg> element
// const svg = d3.select('section').append('svg')
//     .attr('width', width + margin.left + margin.right)
//     .attr('height', height + margin.top + margin.bottom);

// const g = svg.append('g')
//     .attr('transform', `translate(${margin.left},${margin.top})`);


// Global variable for all data
//let priceRangesData;

// async function getDataPrice(endpoint) {
//     try {
//         priceRangesData = await d3.json(endpoint);
//         console.table(priceRangesData);
//         return priceRangesData;
//     } catch (error) {
//         console.log(error);
//     }
// }
// getDataPrice(endpoint)

